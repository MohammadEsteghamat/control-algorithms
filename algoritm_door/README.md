# 📘 Multi-Stage Sigmoid Model  
**مدل سیگموئیدی چندمرحله‌ای** — یک مدل تحلیلی پیشرفته برای رفتارهای چندفازی

[![Language](https://img.shields.io/badge/Language-Math%20%26%20Python-blue)](https://www.python.org/)
[![Applications](https://img.shields.io/badge/Applications-Growth%20%7C%20Biology%20%7C%20Control-green)]( )
[![Status](https://img.shields.io/badge/Status-Stable-brightgreen)]( )
[![License](https://img.shields.io/github/license/yourusername/Multi-Stage-Sigmoid-Model)](LICENSE)

---

## 🔥 معرفی

مدل سیگموئیدی چندمرحله‌ای یک مدل تحلیلی دقیق است که با ترکیب چند تابع لجستیک (Sigmoid) و یک مؤلفهٔ پایه، قادر به توصیف پدیده‌های **دو یا چند مرحله‌ای** می‌باشد — پدیده‌هایی که یک منحنی S شکل ساده برای آن‌ها کافی نیست (مثل رشد بیولوژیکی با plateau موقت، پذیرش فناوری در دو موج، یادگیری انسانی با فازهای تثبیت و غیره).

این مدل گذارهای بسیار نرم و قابل کنترل بین مراحل دارد و دارای **انتگرال تحلیلی بسته (closed-form)** است.

---

## 🎯 ویژگی‌های کلیدی

- مدل‌سازی دقیق رفتارهای دوفازی و چندفازی  
- ترکیب خطی چندین تابع لجستیک با نقاط فعال‌سازی متفاوت  
- رفتار کاملاً صاف (C^∞) و بدون ناپیوستگی  
- انتگرال تحلیلی دقیق → بسیار مناسب برای کنترل، بهینه‌سازی و شبیه‌سازی  
- کاربرد در بیولوژی، اقتصاد، فناوری، روان‌شناسی و مهندسی کنترل

---

## 📐 پارامترهای مدل

| پارامتر  | توضیح                              |
|---------|-----------------------------------|
| `A`     | حد بالای موقت فاز اول (plateau میانی) |
| `y₁`    | مقدار اولیه (در x = 0)              |
| 
| `y₂`    | مقدار نهایی در حالت پایدار          |
| `k`     | نرخ رشد / شیب سیگموئیدها (بزرگ‌تر → تندتر) |
| `b`     | نقطهٔ شروع فاز دوم (تأخیر فاز دوم) |
| `x`     | متغیر مستقل (معمولاً زمان)         |

---

## 🧩 تابع اصلی — دوفازی (g(x))

$$  
g(x) = (A - y_1) \left( \frac{1}{1 + e^{-kx}} - \frac{1}{1 + e^{-k(x-b)}} \right) + (y_2 - A) \frac{1}{1 + e^{-k(x-b)}} + y_1
\quad \text{for } x \geq 0
  $$

### رفتار تابع:
1. فاز اول: رشد سریع از `y₁` به سمت `A`  
2. فاز میانی: توقف یا کندی موقت در نزدیکی `A`  
3. فاز دوم: رشد نهایی از `A` به سمت `y₂`

(با تنظیم `b` و `k` می‌توان فاز میانی را کاملاً حذف کرد و مدل یک‌سیگموئیدی ساده شد.)

---

## 🧱 اجزای تشکیل‌دهنده

$$  
\begin{aligned}
f_1(x) &= \frac{A - y_1}{1 + e^{-kx}} \\[6pt]
f_2(x) &= -\frac{A - y_1}{1 + e^{-k(x-b)}} \\[6pt]
f_3(x) &= \frac{y_2 - A}{1 + e^{-k(x-b)}} \\[6pt]
f_4(x) &= y_1 \\[6pt]
g(x) &= f_1(x) + f_2(x) + f_3(x) + f_4(x)
\end{aligned}
  $$

---

## 🟢 انتگرال تحلیلی دقیق (Antiderivative)

هر جزء لجستیک انتگرال بسته دارد:

$$  
\int \frac{1}{1 + e^{-k(x-c)}} dx = \frac{1}{k} \ln(1 + e^{k(x-c)}) + C
  $$

بنابراین انتگرال کل مدل (با ثابت ادغام مناسب):

$$  
\boxed{
\begin{aligned}
G(x) &= (A - y_1)\frac{1}{k} \ln \left( \frac{1 + e^{kx}}{1 + e^{k(x-b)}} \right) \\
&\quad + (y_2 - A)\frac{1}{k} \ln(1 + e^{k(x-b}) \\
&\quad + y_1 \, x + C
\end{aligned}
}
  $$

> این فرم بسیار تمیز، دقیق و بدون عدد جادویی است!

---

## 📊 مثال عددی (پایتون)

```python
import numpy as np
import matplotlib.pyplot as plt

def multi_stage_sigmoid(x, y1=0, A=8, y2=20, k=0.5, b=10):
    term1 = (A - y1) * (1/(1 + np.exp(-k*x)) - 1/(1 + np.exp(-k*(x-b))))
    term2 = (y2 - A) * (1/(1 + np.exp(-k*(x-b))))
    return term1 + term2 + y1

x = np.linspace(0, 30, 1000)
y = multi_stage_sigmoid(x, y1=1, A=10, y2=25, k=0.6, b=12)

plt.plot(x, y, label='Two-Phase Sigmoid', lw=2.5)
plt.grid(alpha=0.3)
plt.title('Multi-Stage Sigmoid Model')
plt.xlabel('Time')
plt.ylabel('Value')
plt.legend()
plt.show()
