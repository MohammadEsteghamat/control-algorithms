# 📘 مدل تابع سیگموئیدی چندمرحله‌ای (Multi-Stage Sigmoid Model)

این پروژه یک مدل تحلیلی پیشرفته بر پایه **توابع لجستیک (سیگموئید)** و انتگرال‌های آن‌ها ارائه می‌دهد که قادر به مدل‌سازی رفتارهای **دو فاز یا چندمرحله‌ای** با دقت بالا است.

مدل نهایی (`g(x)`) از ترکیب هوشمندانه‌ی چندین تابع سیگموئیدی و یک بخش خطی تشکیل شده و برای شبیه‌سازی فرآیندهای واقعی با گذار نرم بین مراحل مختلف بسیار مناسب است.

<div align="center">
  <img src="https://img.shields.io/badge/Language-Math%20%26%20LaTeX-blue.svg" alt="Language">
  <img src="https://img.shields.io/badge/Application-Growth%20Modeling%20%7C%20Control%20%7C%20Biology-green" alt="Applications">
  <img src="https://img.shields.io/badge/Status-Stable-brightgreen" alt="Status">
</div>

---

## ⭐ هدف مدل

این مدل برای شبیه‌سازی فرآیندهایی طراحی شده که دارای **رفتار دو یا چند مرحله‌ای** هستند، مانند:

- رشد تدریجی جمعیت، یادگیری، یا فناوری (مانند منحنی S دوگانه)
- مدل‌سازی پاسخ سیستم‌های زیستی (مانند فعال‌سازی نورون‌ها یا پاسخ دارویی)
- کنترل تطبیقی و گذار نرم بین دو حالت پایدار
- شبیه‌سازی فرآیندهای صنعتی با فاز تأخیری و فاز اشباع

---

## 📐 پارامترهای مدل

| پارامتر   | توضیحات                                      |
|-----------|----------------------------------------------|
| `A`       | حد بالای مرحله اول (سقف موقت رشد اولیه)      |
| `y₁`      | مقدار اولیه / پایه تابع                      |
| `y₂`      | مقدار نهایی در حالت پایدار (پس از فاز دوم)   |
| `k`       | ضریب شیب سیگموئید (سرعت گذار)               |
| `b`       | نقطه شروع فاز دوم (تأخیر زمانی فاز دوم)      |
| `x`       | متغیر مستقل (معمولاً زمان یا ورودی)          |

---

## 🟦 تابع اصلی – g(x)

```math
g(x) = \begin{cases} x \geq 0: \\\quad (A - y_1) \left( \dfrac{1}{1 + e^{-kx}} - \dfrac{1}{1 + e^{-k(x-b)}} \right) \\\quad + (y_2 - y_1) \left( \dfrac{1}{1 + e^{-k(x-b)}} \right) + y_1
\end{cases}
```
این تابع یک رشد اولیه سریع تا A، سپس کاهش موقت و در نهایت گذار نرم به y₂ را مدل می‌کند.

🟩 توابع جزء (اجزای سازنده)

























تابعفرمولf(x)$\dfrac{A - y_1}{1 + e^{-kx}}$f₁(x)$- \dfrac{A - y_1}{1 + e^{-k(x-b)}}$f₂(x)$(y_2 - y_1) \left( \dfrac{1}{1 + e^{-k(x-b)}} \right)$f₃(x)$y_1$
بنابراین: $ g(x) = f(x) + f_1(x) + f_2(x) + f_3(x) $

🟧 انتگرال‌ها (Primitive / Antiderivative Functions)

























انتگرالفرمول$F(x)$$\dfrac{A - y_1}{k} \ln(1 + e^{kx}) - 1.75480298876$$F_1(x)$$- \dfrac{A - y_1}{k} \ln(1 + e^{k(x-b)})$$F_2(x)$$\dfrac{y_2 - y_1}{k} \ln(1 + e^{k(x-b)})$$F_3(x)$$y_1 x$

🟥 تابع نهایی (انتگرال کلی مدل) – F₄(x)
$$F_4(x) = F(x) + F_1(x) + F_2(x) + F_3(x) + 7.7348261129 \times 10^{-10}
$$
این تابع انتگرال تحلیلی دقیق از $g(x)$ است (با تصحیح ثابت‌های عددی برای تطابق کامل).

📊 رفتار مدل (به طور خلاصه)

فاز اول (x ≈ 0 تا b): رشد سریع سیگموئیدی تا نزدیک A
فاز میانی: اصلاح و کاهش موقت (به دلیل فعال شدن سیگموئید دوم)
فاز دوم (x > b): گذار نرم و نهایی به سمت y₂

این رفتار برای مدل‌سازی پدیده‌هایی مثل یادگیری انسانی، پذیرش فناوری در دو موج، یا رشد تومور با فاز نهفتگی و سپس متاستاز بسیار مناسب است.

🚀 کاربردها

تحلیل داده‌های رشد دو مرحله‌ای
مدل‌سازی یادگیری عمیق (Deep Learning) در انسان
کنترل سیستم‌های غیرخطی با گذار فازی
شبیه‌سازی فرآیندهای بیولوژیکی پیچیده


👨‍💻 نویسنده
این مدل توسط [نام شما] توسعه داده شده است.
در صورت علاقه به همکاری یا استفاده در پروژه‌های تحقیقاتی، خوشحال می‌شوم بشنوم!


مدل سیگموئیدی چندمرحله‌ای – وقتی یک S کافی نیست! 🚀

```

