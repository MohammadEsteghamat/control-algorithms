# 📘 مدل تابع سیگموئیدی چندمرحله‌ای (Multi-Stage Sigmoid Model)

مدلی تحلیلی و دقیق برای شبیه‌سازی فرآیندهای واقعی با **رفتار دو یا چند مرحله‌ای** بر پایه ترکیب هوشمند چندین تابع سیگموئید و انتگرال‌های بستهٔ آن‌ها.

<div align="center">

[![Language](https://img.shields.io/badge/Language-Math%20%26%20LaTeX-blue.svg)](https://en.wikipedia.org/wiki/LaTeX)
[![Applications](https://img.shields.io/badge/Applications-Growth%20%7C%20Biology%20%7C%20Control-green)](https://en.wikipedia.org/wiki/Sigmoid_function)
[![Status](https://img.shields.io/badge/Status-Stable-brightgreen.svg)]()
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

</div>

---
## ✨ هدف مدل
شبیه‌سازی پدیده‌هایی که دارای **چند فاز مشخص** با گذار نرم هستند:
- رشد جمعیت، یادگیری انسانی یا پذیرش فناوری با دو منحنی S متوالی
- پاسخ دوزی دارو با فاز نهفتگی و سپس فعال‌سازی قوی
- یادگیری عمیق در انسان (پیشرفت سریع → پلاتو → شتاب دوباره)
- کنترل سیستم‌های غیرخطی با گذار نرم بین حالت‌های پایدار
- رشد تومور با فاز اولیه، نهفتگی و متاستاز

> وقتی یک منحنی S کافی نیست! 🚀

---
## 📐 پارامترهای مدل

| پارامتر | توضیحات                                           |
|--------|---------------------------------------------------|
| `A`    | سقف موقت رشد در مرحله اول (حداکثر مقدار فاز اولیه) |
| `y₁`   | مقدار اولیه (پایه)                               |
| `y₂`   | مقدار نهایی در حالت پایدار (پس از فاز دوم)       |
| `k`    | نرخ رشد / شیب سیگموئید (سرعت گذار)             |
| `b`    | نقطه شروع فاز دوم (تأخیر یا آستانه فعال‌سازی)   |
| `x`    | متغیر مستقل (معمولاً زمان یا ورودی)             |

---
## 🟦 تابع اصلی – g(x)

$$g(x) = (A - y_1) \left( \frac{1}{1 + e^{-kx}} - \frac{1}{1 + e^{-k(x-b)}} \right) + (y_2 - y_1) \frac{1}{1 + e^{-k(x-b)}} + y_1 \quad \text{for } x \geq 0$$

### اجزای سازنده
$$
\begin{aligned}
&f(x) \;=\; \frac{A - y_1}{1 + e^{-kx}} \\[10pt]
&f_1(x) \;=\; -\frac{A - y_1}{1 + e^{-k(x-b)}} \\[10pt]
&f_2(x) \;=\; (y_2 - y_1) \frac{1}{1 + e^{-k(x-b)}} \\[10pt]
&f_3(x) \;=\; y_1 \\[10pt]
&g(x) \;=\; f(x) + f_1(x) + f_2(x) + f_3(x)
\end{aligned}
$$

### رفتار مدل
- فاز ۱ (x ≈ ۰ تا b): رشد سریع سیگموئیدی تا نزدیک A  
- فاز میانی: اصلاح موقت، پلاتو یا کاهش جزئی  
- فاز ۲ (x > b): گذار نرم و نهایی به سمت y₂

---
## 🟧 انتگرال تحلیلی (Primitive Function)

$$
\begin{aligned}
F(x)   &= \frac{A - y_1}{k} \ln(1 + e^{kx}) \\[8pt]
F_1(x) &= -\frac{A - y_1}{k} \ln(1 + e^{k(x-b)}) \\[8pt]
F_2(x) &= \frac{y_2 - y_1}{k} \ln(1 + e^{k(x-b)}) \\[8pt]
F_3(x) &= y_1 x
\end{aligned}
$$

### انتگرال کلی مدل – F₄(x)
$$F_4(x) = \frac{A - y_1}{k} \ln(1 + e^{kx}) + \left( \frac{y_2 - A}{k} \right) \ln(1 + e^{k(x-b)}) + y_1 x + C$$

ثابت C معمولاً بسیار کوچک است (≈ 7.73 × 10⁻¹⁰) و در اکثر کاربردها می‌توان آن را صفر در نظر گرفت.

---
## 📊 مثال پارامترهای نمونه
```python
A  = 100
y1 = 10
y2 = 200
k  = 0.5
b  = 15
