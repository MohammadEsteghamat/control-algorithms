# 📘 مدل تابع سیگموئیدی چندمرحله‌ای

این فایل شامل تعریف یک مدل تحلیلی بر پایه توابع لجستیک (Logistic
Functions) و انتگرال‌های آن‌ها است.
مدل از چهار تابع جزء تشکیل شده و در نهایت یک تابع نهایی ( F_4(x) ) به‌دست
می‌آید.

------------------------------------------------------------------------

## ⭐ هدف مدل

هدف این توابع، مدل‌سازی یک رفتار **دو فاز** یا **چندمرحله‌ای** با استفاده
از اختلاف دو سیگموئید و ترکیب آن با یک بخش خطی است.
این نوع مدل‌سازی معمولاً در موارد زیر کاربرد دارد:

-   تحلیل رشد تدریجی یک فرآیند
-   مدل‌سازی ورودی‌--‌خروجی سیستم‌های زیستی
-   کنترل تطبیقی
-   مدل‌سازی گذار بین دو مقدار پایدار

------------------------------------------------------------------------

# 📐 تعریف پارامترها

  پارامتر   توضیح
  --------- -----------------------------
  ( A )     حد بالای مرحله اول تابع
  ( y_1 )   مقدار اولیه/پایه تابع
  ( y_2 )   مقدار نهایی مرحله دوم تابع
  ( k )     ضریب شیب سیگموئید
  ( b )     نقطه شروع فاز دوم سیگموئیدی
  ( x )     متغیر مستقل

------------------------------------------------------------------------

# 🟦 تابع اصلی ( g(x) )

``` math
g(x) = \{ x \ge 0 : (A - y_1)\left(rac{1}{1 + e^{-kx}}
      - rac{1}{1 + e^{-k(x-b)}}ight)
      + (y_2 - y_1)\left(rac{1}{1 + e^{-k(x-b)}}ight) + y_1 \}
```

------------------------------------------------------------------------

# 🟩 توابع جزء

## 1. تابع اول

``` math
f(x) = \{ x \ge 0 : rac{A - y_1}{1 + e^{-kx}} \}
```

## 2. تابع اصلاح‌کننده

``` math
f_1(x) = \{ x \ge 0 : -rac{A - y_1}{1 + e^{-k(x-b)}} \}
```

## 3. تابع مرحله دوم

``` math
f_2(x) = \{ x \ge 0 : (y_2 - y_1)\left(rac{1}{1 + e^{-k(x-b)}}ight) \}
```

## 4. تابع پایه

``` math
f_3(x) = \{ x \ge 0 : y_1 \}
```

------------------------------------------------------------------------

# 🟧 انتگرال‌ها (Primitive Functions)

## 1. انتگرال تابع اول

``` math
F(x) = rac{A - y_1}{k}\ln(1 + e^{kx}) - 1.75480298876
```

## 2. انتگرال تابع اصلاح‌کننده

``` math
F_1(x) = -rac{A - y_1}{k}\ln(1 + e^{k(x-b)})
```

## 3. انتگرال مرحله دوم

``` math
F_2(x) = rac{y_2 - y_1}{k}\ln(1 + e^{k(x-b)})
```

## 4. انتگرال تابع پایه

``` math
F_3(x) = y_1 x
```

------------------------------------------------------------------------

# 🟥 ترکیب نهایی

``` math
F_4(x) = F_1(x) + F_2(x) + F(x) + F_3(x)
        + 7.7348261129*10^{-10}
```

------------------------------------------------------------------------
