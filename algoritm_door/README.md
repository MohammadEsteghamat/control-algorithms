# 📘 مدل تابع سیگموئیدی چندمرحله‌ای

این فایل شامل تعریف یک مدل تحلیلی بر پایه توابع لجستیک (Logistic Functions) و انتگرال‌های آن‌ها است.  
مدل از چهار تابع جزء تشکیل شده و در نهایت یک تابع نهایی \( F_4(x) \) به‌دست می‌آید.

---

## ⭐ هدف مدل  
هدف این توابع، مدل‌سازی یک رفتار **دو فاز** یا **چندمرحله‌ای** با استفاده از اختلاف دو سیگموئید و ترکیب آن با یک بخش خطی است.  
این نوع مدل‌سازی معمولاً در موارد زیر کاربرد دارد:

- تحلیل رشد تدریجی یک فرآیند  
- مدل‌سازی ورودی‌–‌خروجی سیستم‌های زیستی  
- کنترل تطبیقی  
- مدل‌سازی گذار بین دو مقدار پایدار  

---

# 📐 تعریف پارامترها

| پارامتر | توضیح |
|--------|-------|
| \( A \) | حد بالای مرحله اول تابع |
| \( y_1 \) | مقدار اولیه/پایه تابع |
| \( y_2 \) | مقدار نهایی مرحله دوم تابع |
| \( k \) | ضریب شیب سیگموئید |
| \( b \) | نقطه شروع فاز دوم سیگموئیدی |
| \( x \) | متغیر مستقل |

---

# 🟦 تابع اصلی \( g(x) \)

```math
g(x) = \{ x \ge 0 : (A - y_1)\left(\frac{1}{1 + e^{-kx}}
      - \frac{1}{1 + e^{-k(x-b)}}\right)
      + (y_2 - y_1)\left(\frac{1}{1 + e^{-k(x-b)}}\right) + y_1 \}
